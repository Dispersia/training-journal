name: repositories
description: Repositories for training journal
version: 0.0.1
author:
homepage:

publish_to: 'none'

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  protobuf: ^1.0.1
  grpc: ^2.9.0

dev_dependencies:
